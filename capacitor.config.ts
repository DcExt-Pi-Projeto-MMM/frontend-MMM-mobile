import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MudaMeuMundo_teste',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
